export const Statuses: {[k: string]: ModdedPureEffectData} = {
	/*silvally: {
		name: 'Silvally',
		id: 'silvally',
		num: 773,
		onAbilityPriority: 1,
		onAbility(pokemon) {
			if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvallybug') {
				pokemon.ability === 'tintedlens',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			else if (pokemon.species.id === 'silvally') {
				pokemon.ability === 'adaptability',
			}
			return [ability];
		},
	},*/
};
